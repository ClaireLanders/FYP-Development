// Organisation Registration tab screen (US11 & US12)
// Renders the OrgRegistrationForm component for setting up new organisations
import { StyleSheet } from 'react-native';
import { OrgRegistrationForm } from '@/components/Registration/OrgRegistrationForm';

export default function RegisterScreen() {
  return <OrgRegistrationForm />;
}

const styles = StyleSheet.create({});